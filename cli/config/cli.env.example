# BMC CLI Environment Variables
# Copy this file to .env or cli.env and set appropriate values

# =============================================================================
# Manager Connection
# =============================================================================
BMC_MANAGER_ENDPOINT=http://localhost:8080
# The BMC Manager service endpoint
# Maps to: manager.endpoint in config.yaml

# =============================================================================
# Optional - Legacy Gateway URL (for backward compatibility)
# =============================================================================
# BMC_GATEWAY_URL=http://localhost:8081
# Maps to: gateway.url in config.yaml

# =============================================================================
# Optional - Authentication (alternative to login command)
# =============================================================================
# BMC_AUTH_ACCESS_TOKEN=your-jwt-token-here
# Maps to: auth.access_token in config.yaml

# BMC_AUTH_REFRESH_TOKEN=your-refresh-token-here
# Maps to: auth.refresh_token in config.yaml

# BMC_AUTH_API_KEY=your-api-key-here
# Maps to: auth.api_key in config.yaml

# BMC_AUTH_EMAIL=user@example.com
# Maps to: auth.email in config.yaml

# =============================================================================
# How Environment Variables Work
# =============================================================================
# The CLI uses the BMC_ prefix for all environment variables.
# Nested config keys use underscores instead of dots:
#   manager.endpoint     → BMC_MANAGER_ENDPOINT
#   auth.access_token    → BMC_AUTH_ACCESS_TOKEN
#   gateway.url          → BMC_GATEWAY_URL
